# Normalize text files to LF to reduce cross-platform merge noise
* text=auto eol=lf

# Keep HTML/CSS/JS consistently LF to prevent unnecessary conflict hunks
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
